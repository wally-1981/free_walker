POST /_search/template/test_template
{
    "template": {
        "query": {
        	"range": {
        		"deadline_datetime": {
        			"gte": "0"
        		}
        	}
        },
        "from": "{{from}}",
        "size": "{{size}}"
    }
}